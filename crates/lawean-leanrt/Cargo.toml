[package]
name = "lawean-leanrt"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Call the proved Lean definitions (Ident.applyUnit, checkUnit) from Rust via Lean's C backend (ADR-0014 route 1)"
build = "build.rs"

[dependencies]
lawean-amend = { path = "../lawean-amend" }

[build-dependencies]
cc = "1"

[dev-dependencies]
lawean-source = { path = "../lawean-source" }
