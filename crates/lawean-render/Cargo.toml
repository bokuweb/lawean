[package]
name = "lawean-render"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Reverse direction: amendment ops -> 改め文, Semantic IR -> Japanese, for human review and structured authoring"

[dependencies]
lawean-source = { path = "../lawean-source" }
lawean-semantic = { path = "../lawean-semantic" }
lawean-resolve = { path = "../lawean-resolve" }
lawean-amend = { path = "../lawean-amend" }
